# Hetzner Cloud

Hetzner Cloud servers via REST API. [Hetzner Cloud](https://www.hetzner.com/cloud/)

## Agents

#### Claude Code

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/claude.sh)
```

#### OpenClaw

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/openclaw.sh)
```

#### Codex CLI

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/codex.sh)
```

#### OpenCode

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/opencode.sh)
```

#### Kilo Code

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/kilocode.sh)
```

#### Hermes

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/hermes.sh)
```

#### Junie

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/junie.sh)
```

#### Cursor CLI

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/cursor.sh)
```

#### Pi

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/pi.sh)
```

#### T3 Code

```bash
bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/t3code.sh)
```

## Non-Interactive Mode

```bash
HETZNER_SERVER_NAME=dev-mk1 \
HCLOUD_TOKEN=your-token \
THEGRID_API_KEY=sk-or-v1-xxxxx \
  bash <(curl -fsSL https://spawn.thegrid.ai/hetzner/claude.sh)
```
