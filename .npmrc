; Fail fast when the toolchain is too old instead of producing confusing
; downstream errors (e.g. Node 18 breaking the Hermes dashboard build).
; Honors the "engines" field in package.json (node >=20.19.0, npm >=10).
engine-strict=true
